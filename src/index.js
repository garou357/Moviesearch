import React from 'react';
import ReactDOM from 'react-dom';


import App from './App';
ReactDOM.render( //ReactDOM render takes two arguments: the component to render and the DOM element to render it in.
  <App />,
  document.getElementById('root')
);
